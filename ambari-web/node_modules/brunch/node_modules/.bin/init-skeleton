#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../init-skeleton/init-skeleton" "$@"
  ret=$?
else 
  node  "$basedir/../init-skeleton/init-skeleton" "$@"
  ret=$?
fi
exit $ret
